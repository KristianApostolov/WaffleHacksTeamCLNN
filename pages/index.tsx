import type { NextPage } from "next";
import Root from "components/Root";

const Home: NextPage = () => {
    return (
        <Root header>
            <h1>Hello Next.js</h1>
        </Root>
    );
};

export default Home;
